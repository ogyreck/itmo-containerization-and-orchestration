apiVersion:  monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: "{{ .Release.Name }}-monitor"
spec:
  selector:
    matchLabels:
      app: lab-api
  endpoints:
    - port: web          
      path: /metrics     
      interval: 15s      
